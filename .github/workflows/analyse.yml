name: worker

on:
  push:

jobs:
  wrap:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        fibre: ["summit", "expert", "weave", "communication", "fill"]
        affectionate: ["previous", "robot", "ignorance", "pack"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar pP
